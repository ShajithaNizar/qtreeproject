version: '3'
services: #it denotes container
  qtreel: 
    image: nginx:latest  
    ports:
      - "8085:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data:
