version: '3'
services: #it denotes container
  qtree_1: 
    image: ubuntu:latest  
    ports:
      - "8050:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data: 
