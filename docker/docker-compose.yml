version: '3'
services: #it denotes container
  container_name: 
    image: image_name:latest  
    ports:
      - "8050:80" 
    volumes:
      - volume_name:/app-data 
    networks:
      - network_name
networks:
  network_name: 
    driver: bridge 
volumes:
  volume_name: 
