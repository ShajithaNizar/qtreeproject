version: '3'
services: #it denotes container
  qtree_4: 
    image: ubuntu:latest  
    ports:
      - "8050:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data: 
