version: '3'
services: #it denotes container
  new_cont: 
    image: nginx:latest  
    ports:
      - "80880:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data:
