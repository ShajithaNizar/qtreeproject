version: '3'
services: #it denotes container
  qtree: 
    image: nginx:latest  
    ports:
      - "8082:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data:
