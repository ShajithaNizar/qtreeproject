version: '3'
services: #it denotes container
  container_name: 
    image: image_name:latest  
    ports:
      - "port:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  name: 
    driver: bridge 
volumes:
  name: 
