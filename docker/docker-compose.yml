version: '3'
services: #it denotes container
  new_container: 
    image: nginx:latest  
    ports:
      - "8088:80" 
    volumes:
      - data:/app-data 
    networks:
      - qtree
networks:
  qtree: 
    driver: bridge 
volumes:
  data:
